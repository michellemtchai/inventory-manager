<%= form_with(model: city) do |form| %>
  <% if city.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
        <% city.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :region %><br>
    <%= form.text_field :region %>
  </div>
  <div>
    <%= form.label :country_id %><br>
    <%= form.collection_select :country_id, Country.order(:name),:id,:name %>
  </div>
  <div>
    <%= form.label :lat %><br>
    <%= form.number_field :lat, step: 0.5 %>
  </div>
  <div>
    <%= form.label :long %><br>
    <%= form.number_field :long, step: 0.5 %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
